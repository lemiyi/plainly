import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary brand color for the app: #2f84a5
  static const Color primary = Color(0xFF2F84A5);

  /// Neutral background color (can be adjusted later)
  static const Color background = Color(0xFFF7F9FB);
}
